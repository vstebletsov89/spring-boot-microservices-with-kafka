spring:
  application:
    name: "Test env for order-client"

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
